@inherits UmbracoTemplatePage
<article id="@Model.Content.Name.Replace(' ', '-')" class="section-wrapper clearfix" data-custom-background-img="/images/bg2.jpg">
    <div class="content-wrapper clearfix">
        <div class="col-sm-11 col-md-10 pull-right">
            <h1 class="section-title">@Umbraco.Field("Title")</h1>
            <section class="grid row clearfix clearfix-for-2cols">
                @foreach (var child in Model.Content.Children)
                {
                    @Umbraco.RenderTemplate(child.Id)
                }
            </section>
        </div>
    </div>
</article>